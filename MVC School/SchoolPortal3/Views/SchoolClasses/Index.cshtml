@model IEnumerable<SchoolPortal3.Entities.SchoolClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClassName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgramManagerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClassName)
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.ProgramManager.DisplayName)*@
                @(item.ProgramManager.DisplayName?.ToString() ?? "Unknown")
            </td>
            <td>
                @*@String.Format("{0:yyyy/MM/dd}", item.Created)*@
                @(String.Format("{0:yyyy/MM/dd}", item.Created) ?? "Unknown")
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
